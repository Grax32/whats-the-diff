{
  "name": "whats-the-diff",
  "version": "1.0.0",
  "description": "Comparison tool intended to keep base files in sync when applications use the same configuration",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "cp index.html dist/ && cp styles.css dist/ && cp config.example.json dist/",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron ."
  },
  "keywords": [
    "electron",
    "directory",
    "comparison",
    "sync"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.2.3",
    "electron": "^40.4.1",
    "typescript": "^5.9.3"
  }
}
